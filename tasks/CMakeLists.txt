# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(camera_rtsp_gstreamerTaskLib)
ADD_LIBRARY(${CAMERA_RTSP_GSTREAMER_TASKLIB_NAME} SHARED
    ${CAMERA_RTSP_GSTREAMER_TASKLIB_SOURCES})
add_dependencies(${CAMERA_RTSP_GSTREAMER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${CAMERA_RTSP_GSTREAMER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${CAMERA_RTSP_GSTREAMER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${CAMERA_RTSP_GSTREAMER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${CAMERA_RTSP_GSTREAMER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${CAMERA_RTSP_GSTREAMER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${CAMERA_RTSP_GSTREAMER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${CAMERA_RTSP_GSTREAMER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${CAMERA_RTSP_GSTREAMER_TASKLIB_HEADERS}
    DESTINATION include/orocos/camera_rtsp_gstreamer)

